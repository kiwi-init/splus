{
  "name": "splus",
  "version": "0.1.0",
  "private": true,
  "description": "Splus — the precision-first code reviewer. One deterministic engine, three surfaces: GitHub App, local CLI, web dashboard.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "engine:build": "cargo build --release",
    "engine:test": "cargo test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
